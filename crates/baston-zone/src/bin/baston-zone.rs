//! Standalone zone-server binary. Phase A runs gateway+zone in a single process
//! (`baston-gateway`); this binary is scaffolding for the Phase D split.

fn main() {
    println!("baston-zone: standalone zone server is scaffolded for Phase D; run baston-gateway.");
}
