target/
.git/
# Machine-local cargo config (Windows target-dir breaks the Linux builder).
.cargo/
*.pem
docs/
# The documentation website is a separate app; a server image never needs it.
apps/
deploy/monitoring/
**/node_modules/
